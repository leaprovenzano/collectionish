# Config file for automatic testing at travis-ci.org
language: python
python:
  - "3.6"
  - "3.7"
  - "3.8"
# command to install dependencies
install:
  - pip install -Ur requirements/test.txt
  - pip install .
# command to run tests
script: py.test --cov src/collectionish